---
"@temporal-contract/contract": minor
"@temporal-contract/worker": minor
"@temporal-contract/client": minor
"@temporal-contract/testing": minor
---

Bump runtime dependencies: `testcontainers` 11 → 12 and `@temporalio/*` 1.17 → 1.18 in `@temporal-contract/testing`. Peer ranges (`@temporalio/*` `^1`, `neverthrow` `^8`) are unchanged.
